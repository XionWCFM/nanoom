---
"@nanoom/cli": minor
---

Simplify the GitHub status Action to aggregate all `needs` results directly. The Action now accepts only `needs`, treats `success` and `skipped` as passing, and rejects failed, cancelled, missing, or unknown results.
